server:
  port: 5000

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: order
          uri: lb://order
          predicates:
            - Path=/order/**

eureka:
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:4000/eureka